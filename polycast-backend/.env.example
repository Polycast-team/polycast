# Server Configuration
PORT=8080
NODE_ENV="development"

# API Key Configuration
GEMINI_API_KEY="your_gemini_api_key_here"
GOOGLE_SPEECH_RECOGNIZER="projects/your-project/locations/us/recognizers/your-recognizer"
GOOGLE_SPEECH_REGION="us"
GOOGLE_SPEECH_ENDPOINT="us-speech.googleapis.com"
OPENAI_API_KEY="your_openai_api_key_here"
# YouTube Data API v3 key (OPTIONAL - not needed for video search)
# Video search now uses InnerTube API (no API key required)
# YOUTUBE_API_KEY="your_youtube_api_key_here"

# Redis Configuration (optional for local development)
# REDIS_URL="redis://localhost:6379"

# CORS Configuration (optional - defaults are built-in)
# Add custom origins as comma-separated list if needed
# ALLOWED_ORIGINS="https://custom-domain.com,https://another-domain.com"

# Chrome Extension IDs (comma-separated)
# Get your extension ID from chrome://extensions/ page
ALLOWED_EXTENSION_IDS="oflmpbnhpjheifdgcpclpoijbfhjinfk"
