# Development environment variables
# These use window.location to adapt to the current protocol/host

# When accessed via localhost:5173 → uses http/ws
# When accessed via ngrok HTTPS → uses https/wss

# Leave these empty to use dynamic URLs
VITE_API_BASE_URL=
VITE_WS_BASE_URL=